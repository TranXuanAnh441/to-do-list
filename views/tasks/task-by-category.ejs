<% for (const [category, categoryTasks] of Object.entries(tasks)) { %>
    <h3><%= category %></h3>
    <hr>
    <% if (categoryTasks.length > 0) { %> 
        <div>
            <% for (let task of categoryTasks) { %>
               <%- include(('./task.ejs'), {task: task}); %>
            <% } %>
        </div>
    <% } else {%>
        <p>No Tasks Found !</p>
    <% } %>
<% } %>